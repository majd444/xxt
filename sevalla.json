{
  "build": {
    "command": "mkdir -p out && cp -r public/* out/",
    "publish": "out",
    "node_version": "18"
  },
  "environment": {
    "NODE_ENV": "production",
    "NEXT_TELEMETRY_DISABLED": "1",
    "DATABASE_URL": "${DATABASE_URL}"
  }
}
