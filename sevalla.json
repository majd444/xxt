{
  "build": {
    "command": "npm run build --force && npm run export",
    "publish": "out",
    "node_version": "18"
  },
  "environment": {
    "NODE_ENV": "production",
    "NEXT_TELEMETRY_DISABLED": "1",
    "DATABASE_URL": "${DATABASE_URL}"
  }
}
