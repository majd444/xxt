{
  "version": 2,
  "framework": "nextjs",
  "buildCommand": "npm install --legacy-peer-deps --force --no-package-lock && npm install date-fns@3.3.1 --save --legacy-peer-deps --no-package-lock && next build",
  "installCommand": "npm install --legacy-peer-deps --force --no-package-lock",
  "regions": ["iad1"],
  "env": {
    "NEXT_TELEMETRY_DISABLED": "1",
    "NODE_ENV": "production"
  },
  "ignoreCommand": "npx --no -- turbo-ignore"
}
