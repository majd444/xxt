{
  "version": 2,
  "framework": "nextjs",
  "buildCommand": "npm install --legacy-peer-deps --force --no-package-lock && npm run build",
  "regions": ["iad1"],
  "env": {
    "NEXT_TELEMETRY_DISABLED": "1",
    "NODE_ENV": "production",
    "OPENROUTER_API_KEY": "REPLACE_WITH_YOUR_OPENROUTER_API_KEY"
  }
}
